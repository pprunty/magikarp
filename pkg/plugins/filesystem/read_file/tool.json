{
  "name": "read_file",
  "description": "Read a UTF-8 text file (≤100 KB) and return its contents",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": { "type": "string" }
    },
    "required": ["path"],
    "additionalProperties": false
  }
} 